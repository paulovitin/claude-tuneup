{
  "name": "claude-tuneup",
  "version": "5.0.0",
  "private": true,
  "type": "module",
  "description": "Audit the instructions your Claude Code install loads every session — and reclaim its disk. Complements the built-in /doctor.",
  "license": "MIT",
  "scripts": {
    "test": "node --test"
  }
}
